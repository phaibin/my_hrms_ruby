<%= form_for(@overtime_state) do |f| %>
  <% if @overtime_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@overtime_state.errors.count, "error") %> prohibited this overtime_state from being saved:</h2>

      <ul>
      <% @overtime_state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
